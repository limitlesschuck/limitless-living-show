Prompt 61c — Wire admin episodes list, leads dashboard, and affiliates page to live categories
Step 1 — Admin episodes list
File to edit: artifacts/nextjs-app/app/admin/(protected)/episodes/page.tsx
Find this exact string:
tsxconst CATEGORY_LABELS: Record<string, string> = {
  grief: "Grief & loss",
  relationship: "Relationship",
  health: "Health & addiction",
  financial: "Financial",
  spiritual: "Spiritual",
  career: "Career & purpose",
};

export default function EpisodesPage() {
  const [episodes, setEpisodes] = useState<Episode[]>([]);
  const [total, setTotal] = useState(0);
  const [filter, setFilter] = useState("");
  const [loading, setLoading] = useState(true);
Replace with:
tsxexport default function EpisodesPage() {
  const [episodes, setEpisodes] = useState<Episode[]>([]);
  const [total, setTotal] = useState(0);
  const [filter, setFilter] = useState("");
  const [loading, setLoading] = useState(true);
  const [categoryLabels, setCategoryLabels] = useState<Record<string, string>>({});
Find this exact string:
tsx  useEffect(() => {
    loadEpisodes(filter);
  }, [filter]);
Replace with:
tsx  useEffect(() => {
    loadEpisodes(filter);
  }, [filter]);

  useEffect(() => {
    fetch("/api/admin/categories")
      .then((r) => r.json())
      .then((data) => {
        const map = Object.fromEntries(
          (data.categories ?? []).map((c: { value: string; label: string }) => [c.value, c.label])
        );
        setCategoryLabels(map);
      });
  }, []);
Find this exact string:
tsx                    <span className="text-xs text-gray-600">
                      {ep.crisisCategory
                        ? CATEGORY_LABELS[ep.crisisCategory] ??
                          ep.crisisCategory
                        : "—"}
                    </span>
Replace with:
tsx                    <span className="text-xs text-gray-600">
                      {ep.crisisCategory
                        ? categoryLabels[ep.crisisCategory] ??
                          ep.crisisCategory
                        : "—"}
                    </span>

Step 2 — Admin leads dashboard
File to edit: artifacts/nextjs-app/app/admin/(protected)/leads/page.tsx
Find this exact string:
tsxconst CATEGORY_LABELS: Record<string, string> = {
  grief: "Grief & loss",
  relationship: "Relationship",
  health: "Health & addiction",
  financial: "Financial",
  spiritual: "Spiritual",
  career: "Career & purpose",
};

const RESULT_STYLES: Record<string, string> = {
Replace with:
tsxconst RESULT_STYLES: Record<string, string> = {
Find this exact string:
tsx  const [guideFilter, setGuideFilter] = useState("");
  const [guideOptions, setGuideOptions] = useState<GuideOption[]>([]);
  const [selected, setSelected] = useState<Lead | null>(null);
Replace with:
tsx  const [guideFilter, setGuideFilter] = useState("");
  const [guideOptions, setGuideOptions] = useState<GuideOption[]>([]);
  const [selected, setSelected] = useState<Lead | null>(null);
  const [categoryLabels, setCategoryLabels] = useState<Record<string, string>>({});
Find this exact string:
tsx  useEffect(() => {
    loadGuideOptions();
  }, []);
Replace with:
tsx  useEffect(() => {
    loadGuideOptions();
    fetch("/api/admin/categories")
      .then((r) => r.json())
      .then((data) => {
        const map = Object.fromEntries(
          (data.categories ?? []).map((c: { value: string; label: string }) => [c.value, c.label])
        );
        setCategoryLabels(map);
      });
  }, []);
Find this exact string:
tsx          <option value="">All categories</option>
          {Object.entries(CATEGORY_LABELS).map(([v, l]) => (
            <option key={v} value={v}>{l}</option>
          ))}
        </select>
Replace with:
tsx          <option value="">All categories</option>
          {Object.entries(categoryLabels).map(([v, l]) => (
            <option key={v} value={v}>{l}</option>
          ))}
        </select>
Find this exact string:
tsx                        <span className="text-xs text-gray-600">
                          {CATEGORY_LABELS[lead.crisisCategory] ?? lead.crisisCategory}
                        </span>
Replace with:
tsx                        <span className="text-xs text-gray-600">
                          {categoryLabels[lead.crisisCategory] ?? lead.crisisCategory}
                        </span>
Find this exact string:
tsx                <DetailRow
                  label="Category"
                  value={CATEGORY_LABELS[selected.crisisCategory] ?? selected.crisisCategory}
                />
Replace with:
tsx                <DetailRow
                  label="Category"
                  value={categoryLabels[selected.crisisCategory] ?? selected.crisisCategory}
                />

Step 3 — Admin affiliates page
File to edit: artifacts/nextjs-app/app/admin/(protected)/affiliates/page.tsx
Find this exact string:
tsxconst CATEGORY_LABELS: Record<string, string> = {
  grief: "Grief & loss",
  relationship: "Relationship",
  health: "Health & addiction",
  financial: "Financial",
  spiritual: "Spiritual",
  career: "Career & purpose",
  default: "— Default fallback —",
};

const URGENCY_LABELS: Record<string, string> = {
Replace with:
tsxconst URGENCY_LABELS: Record<string, string> = {
Find this exact string:
tsxexport default function AffiliatesPage() {
  const [routes, setRoutes] = useState<AffiliateRoute[]>([]);
  const [loading, setLoading] = useState(true);
  const [editing, setEditing] = useState<AffiliateRoute | null>(null);
  const [showAdd, setShowAdd] = useState(false);
  const [form, setForm] = useState(EMPTY_FORM);
  const [saving, setSaving] = useState(false);
  const [message, setMessage] = useState<{ type: "success" | "error"; text: string } | null>(null);

  async function loadRoutes() {
Replace with:
tsxexport default function AffiliatesPage() {
  const [routes, setRoutes] = useState<AffiliateRoute[]>([]);
  const [loading, setLoading] = useState(true);
  const [editing, setEditing] = useState<AffiliateRoute | null>(null);
  const [showAdd, setShowAdd] = useState(false);
  const [form, setForm] = useState(EMPTY_FORM);
  const [saving, setSaving] = useState(false);
  const [message, setMessage] = useState<{ type: "success" | "error"; text: string } | null>(null);
  const [categoryLabels, setCategoryLabels] = useState<Record<string, string>>({});

  async function loadCategories() {
    const res = await fetch("/api/admin/categories");
    const data = await res.json();
    const map = Object.fromEntries(
      (data.categories ?? []).map((c: { value: string; label: string }) => [c.value, c.label])
    );
    setCategoryLabels(map);
  }

  async function loadRoutes() {
Find this exact string:
tsx  useEffect(() => { loadRoutes(); }, []);
Replace with:
tsx  useEffect(() => { loadRoutes(); loadCategories(); }, []);
Find this exact string:
tsx              <label className="block text-xs font-medium text-gray-500 mb-1">Crisis category</label>
              <select value={form.crisisCategory} onChange={(e) => setForm((f) => ({ ...f, crisisCategory: e.target.value }))} className="input">
                {Object.entries(CATEGORY_LABELS).filter(([v]) => v !== "default").map(([v, l]) => (
                  <option key={v} value={v}>{l}</option>
                ))}
              </select>
Replace with:
tsx              <label className="block text-xs font-medium text-gray-500 mb-1">Crisis category</label>
              <select value={form.crisisCategory} onChange={(e) => setForm((f) => ({ ...f, crisisCategory: e.target.value }))} className="input">
                {Object.entries(categoryLabels).map(([v, l]) => (
                  <option key={v} value={v}>{l}</option>
                ))}
              </select>
Find this exact string:
tsx              <RouteTable routes={defaultRoutes} editing={editing} setEditing={setEditing} onToggle={handleToggleActive} onSave={handleSaveEdit} onDelete={handleDelete} saving={saving} />
            </div>
          )}

          {/* Category routes */}
          <div>
            <h2 className="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-3">Category routes</h2>
            <RouteTable routes={categoryRoutes} editing={editing} setEditing={setEditing} onToggle={handleToggleActive} onSave={handleSaveEdit} onDelete={handleDelete} saving={saving} />
Replace with:
tsx              <RouteTable routes={defaultRoutes} editing={editing} setEditing={setEditing} onToggle={handleToggleActive} onSave={handleSaveEdit} onDelete={handleDelete} saving={saving} categoryLabels={categoryLabels} />
            </div>
          )}

          {/* Category routes */}
          <div>
            <h2 className="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-3">Category routes</h2>
            <RouteTable routes={categoryRoutes} editing={editing} setEditing={setEditing} onToggle={handleToggleActive} onSave={handleSaveEdit} onDelete={handleDelete} saving={saving} categoryLabels={categoryLabels} />
Find this exact string:
tsxfunction RouteTable({
  routes,
  editing,
  setEditing,
  onToggle,
  onSave,
  onDelete,
  saving,
}: {
  routes: AffiliateRoute[];
  editing: AffiliateRoute | null;
  setEditing: (r: AffiliateRoute | null) => void;
  onToggle: (r: AffiliateRoute) => void;
  onSave: () => void;
  onDelete: (id: string) => void;
  saving: boolean;
}) {
  const CATEGORY_LABELS: Record<string, string> = {
    grief: "Grief & loss",
    relationship: "Relationship",
    health: "Health & addiction",
    financial: "Financial",
    spiritual: "Spiritual",
    career: "Career & purpose",
  };

  const URGENCY_LABELS: Record<string, string> = {
Replace with:
tsxfunction RouteTable({
  routes,
  editing,
  setEditing,
  onToggle,
  onSave,
  onDelete,
  saving,
  categoryLabels,
}: {
  routes: AffiliateRoute[];
  editing: AffiliateRoute | null;
  setEditing: (r: AffiliateRoute | null) => void;
  onToggle: (r: AffiliateRoute) => void;
  onSave: () => void;
  onDelete: (id: string) => void;
  saving: boolean;
  categoryLabels: Record<string, string>;
}) {
  const URGENCY_LABELS: Record<string, string> = {
Find this exact string:
tsx                  <select value={editing.crisisCategory} onChange={(e) => setEditing({ ...editing, crisisCategory: e.target.value })} className="input text-xs py-1">
                    {Object.entries(CATEGORY_LABELS).map(([v, l]) => <option key={v} value={v}>{l}</option>)}
                  </select>
Replace with:
tsx                  <select value={editing.crisisCategory} onChange={(e) => setEditing({ ...editing, crisisCategory: e.target.value })} className="input text-xs py-1">
                    {Object.entries(categoryLabels).map(([v, l]) => <option key={v} value={v}>{l}</option>)}
                  </select>
Find this exact string:
tsx                  <span className="text-sm text-gray-900">{CATEGORY_LABELS[route.crisisCategory] ?? route.crisisCategory}</span>
Replace with:
tsx                  <span className="text-sm text-gray-900">{categoryLabels[route.crisisCategory] ?? route.crisisCategory}</span>

