## Prompt 30

Add a Captivate Player ID field to the episode schema and edit page, and update the episode detail page to use it for the embedded player.

Step 1 — Add captivatePlayerId to the Prisma schema.

File to edit: artifacts/nextjs-app/prisma/schema.prisma

Find this exact string:
  captivateId           String?   @unique

Replace it with:
  captivateId           String?   @unique
  captivatePlayerId     String?

After saving run:
pnpm --filter @workspace/nextjs-app run db:push


Step 2 — Add captivatePlayerId to the PATCH route allowed fields.

File to edit: artifacts/nextjs-app/app/api/admin/episodes/[id]/route.ts

Find this exact string:
  const allowed = [
    "titleYoutube",
    "titlePodcast",
    "descriptionYoutube",
    "descriptionWebsite",
    "guestName",
    "guestBio",
    "guestEmail",
    "affiliateLink",
    "swipeCopy",
    "systemeContactId",
    "crisisCategory",
    "tags",
    "publishStatus",
    "youtubeId",
    "mp4Url",
    "coverArtUrl",
    "youtubeThumbnailUrl",
    "ctaId",
  ];

Replace it with:
  const allowed = [
    "titleYoutube",
    "titlePodcast",
    "descriptionYoutube",
    "descriptionWebsite",
    "guestName",
    "guestBio",
    "guestEmail",
    "affiliateLink",
    "swipeCopy",
    "systemeContactId",
    "crisisCategory",
    "tags",
    "publishStatus",
    "youtubeId",
    "mp4Url",
    "coverArtUrl",
    "youtubeThumbnailUrl",
    "captivatePlayerId",
    "ctaId",
  ];


Step 3 — Add captivatePlayerId field to the episode edit page.

File to edit: artifacts/nextjs-app/app/admin/(protected)/episodes/[id]/page.tsx

Find this exact string:
  captivateId: string | null;

Replace it with:
  captivateId: string | null;
  captivatePlayerId: string | null;

Find this exact string:
    youtubeId: "",
    mp4Url: "",
    coverArtUrl: "",
    youtubeThumbnailUrl: "",

Replace it with:
    youtubeId: "",
    mp4Url: "",
    coverArtUrl: "",
    youtubeThumbnailUrl: "",
    captivatePlayerId: "",

Find this exact string:
      youtubeId: data.youtubeId ?? "",
      mp4Url: data.mp4Url ?? "",
      coverArtUrl: data.coverArtUrl ?? "",
      youtubeThumbnailUrl: data.youtubeThumbnailUrl ?? "",

Replace it with:
      youtubeId: data.youtubeId ?? "",
      mp4Url: data.mp4Url ?? "",
      coverArtUrl: data.coverArtUrl ?? "",
      youtubeThumbnailUrl: data.youtubeThumbnailUrl ?? "",
      captivatePlayerId: data.captivatePlayerId ?? "",

Find this exact string:
            <Field label="YouTube video ID">
              <input
                type="text"
                value={form.youtubeId}
                onChange={(e) =>
                  setForm((f) => ({ ...f, youtubeId: e.target.value }))
                }
                className="input"
                placeholder="e.g. dQw4w9WgXcQ"
              />
            </Field>

Replace it with:
            <Field label="Captivate player ID">
              <input
                type="text"
                value={form.captivatePlayerId}
                onChange={(e) =>
                  setForm((f) => ({ ...f, captivatePlayerId: e.target.value }))
                }
                className="input"
                placeholder="Paste from Captivate episode editor URL"
              />
              <p className="text-xs text-gray-400 mt-1">
                Found in Captivate as the episode slug e.g. 0f6aa6a5-d1cb-4b66-86bd-9987f951da69
              </p>
            </Field>

            <Field label="YouTube video ID">
              <input
                type="text"
                value={form.youtubeId}
                onChange={(e) =>
                  setForm((f) => ({ ...f, youtubeId: e.target.value }))
                }
                className="input"
                placeholder="e.g. dQw4w9WgXcQ"
              />
            </Field>


Step 4 — Update the episode detail page to use captivatePlayerId for the player, falling back to captivateId.

File to edit: artifacts/nextjs-app/app/episodes/[id]/page.tsx

Find this exact string:
        {/* Captivate embedded player */}
        {episode.captivateId && (
          <div className="mb-8" style={{ width: "100%", height: "200px", borderRadius: "6px", overflow: "hidden" }}>
            <iframe
              style={{ width: "100%", height: "200px" }}
              frameBorder="no"
              scrolling="no"
              allow="clipboard-write"
              src={`https://player.captivate.fm/episode/${episode.captivateId}/`}
            />
          </div>
        )}

Replace it with:
        {/* Captivate embedded player */}
        {(episode.captivatePlayerId ?? episode.captivateId) && (
          <div className="mb-8" style={{ width: "100%", height: "200px", borderRadius: "6px", overflow: "hidden" }}>
            <iframe
              style={{ width: "100%", height: "200px" }}
              frameBorder="no"
              scrolling="no"
              allow="clipboard-write"
              src={`https://player.captivate.fm/episode/${episode.captivatePlayerId ?? episode.captivateId}/`}
            />
          </div>
        )}


Verification: Confirm that:
1. prisma/schema.prisma has captivatePlayerId and db:push completed
2. The PATCH route allows captivatePlayerId
3. The episode edit page has the Captivate player ID field with the helper text
4. The episode detail page uses captivatePlayerId falling back to captivateId
5. TypeScript typecheck passes with zero errors
6. Paste the correct Captivate player ID into episode 3 in the admin, save, and confirm the embedded player on the public episode page now loads the correct episode